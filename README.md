# First-Come-First-Serve
This program implements the FCFS (First Come First Serve) CPU scheduling algorithm in C.  FCFS means: 👉 Processes are executed in the order they arrive 👉 The CPU runs each process until completion (no preemption)
